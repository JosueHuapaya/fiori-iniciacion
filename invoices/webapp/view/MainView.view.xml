<mvc:View xmlns:core="sap.ui.core" controllerName="invoices.invoices.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <content>
        <Panel id="_IDGenPanel"  headerText="{i18n>selectionScreen}" >
            <Label id="labelShipName" labelFor="shipName" text="{i18n>shipName}" class="sapUiSmallMargin"/>
            <Input id="shipName" width="200px" class="sapUiSmallMarginEnd"/>
            <Label id="labelCountry" text="{i18n>country}" class="sapUiSmallMargin"/>
            <Select id="slCountry" selectedKey="{selectionScreen>/CountryKey}" 
                                    forceSelection="false" 
                                    items="{selectionScreen>/Countries}">
            <items>
                <core:Item id="_IDGenItem" key="{selectionScreen>key}" text="{selectionScreen>text}"/>
            </items>
            
            </Select>
        </Panel>
        </content>
    </Page>
</mvc:View>
